# Optional: Kalshi trading account credentials
# Only needed if you want to use authenticated Kalshi endpoints.
# Leave unset to use the public (read-only) API — the default.
#
# KALSHI_API_KEY_ID=your_key_id_here
# KALSHI_PRIVATE_KEY_PATH=/path/to/your/private_key.pem
